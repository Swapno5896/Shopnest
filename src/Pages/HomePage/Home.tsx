import React from 'react';
import SearchProduct from '../SearchProducts/SearchProducts';

const Home = () => {
    return (
        <div>
            <SearchProduct />
        </div>
    );
};

export default Home;